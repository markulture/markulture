---
// Hero Content component
---

<!--
<section class="py-20 bg-gray-100">
  <div class="container mx-auto px-4">
    <h2 class="text-4xl lg:text-5xl font-bold text-center mb-16 leading-tight">
      Our recent<br/>Case Studies
    </h2>

    <div class="grid md:grid-cols-2 gap-8 mb-12">
      <div class="group relative rounded-[2.5rem] overflow-hidden aspect-[4/3] md:aspect-[16/9] cursor-pointer">
        <img 
          src="/images/Frame3.png" 
          alt="Passenger Case Study" 
          class="absolute inset-0 w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent"></div>
        
        <div class="absolute bottom-0 left-0 p-8 md:p-10 w-full">
          <div class="flex gap-3 mb-4">
            <span class="bg-white text-black text-xs font-bold px-4 py-1.5 rounded-full uppercase tracking-wide">Paid Media</span>
            <span class="bg-white text-black text-xs font-bold px-4 py-1.5 rounded-full uppercase tracking-wide">Creative</span>
          </div>
          
          <h3 class="text-3xl md:text-4xl font-bold text-white mb-3">Passenger</h3>
          <p class="text-white/90 text-sm md:text-base max-w-md leading-relaxed">
            Increased Revenue by 10x with Paid Social & Search - Award Winning Campaign
          </p>
        </div>
      </div>

      <div class="group relative rounded-[2.5rem] overflow-hidden aspect-[4/3] md:aspect-[16/9] cursor-pointer">
        <img 
          src="/images/Frame4.png" 
          alt="Beyblade Case Study" 
          class="absolute inset-0 w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
        />
        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent"></div>
        
        <div class="absolute bottom-0 left-0 p-8 md:p-10 w-full">
          <div class="flex gap-3 mb-4">
            <span class="bg-white text-black text-xs font-bold px-4 py-1.5 rounded-full uppercase tracking-wide">Organic Social</span>
            <span class="bg-white text-black text-xs font-bold px-4 py-1.5 rounded-full uppercase tracking-wide">Creative</span>
          </div>
          
          <h3 class="text-3xl md:text-4xl font-bold text-white mb-3">BEYBLADE</h3>
          <p class="text-white/90 text-sm md:text-base max-w-md leading-relaxed">
            Grew +30k followers on TikTok, +393k organic video views, and an 8.21% engagement rate in 6 months.
          </p>
        </div>
      </div>
    </div>

    <div class="flex justify-center">
      <a href="#" class="inline-flex items-center gap-2 bg-black text-white px-8 py-4 rounded-full font-bold hover:bg-gray-800 transition-all duration-300">
        View All Case Studies
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="12" y1="16" x2="12" y2="8"></line>
          <line x1="8" y1="12" x2="16" y2="12"></line>
        </svg>
      </a>
    </div>
  </div>
</section>
-->



<!-- Podcast Section -->
<section class="py-20 bg-white overflow-hidden">
  <div class="container mx-auto px-4">
    <!-- Header -->
    <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-6">
      <h2 class="text-4xl lg:text-5xl font-bold text-gray-900 leading-tight">
        Have you herd?<br/>
        <span class="text-teal-400">Our Social Media podcast</span>
      </h2>
      
      <a href="#" class="inline-flex items-center gap-2 bg-black text-white px-6 py-3 rounded-full font-bold hover:bg-gray-800 transition-all duration-300 text-sm">
        View All Podcasts
        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"></circle>
          <polyline points="12 16 16 12 12 8"></polyline>
          <line x1="8" y1="12" x2="16" y2="12"></line>
        </svg>
      </a>
    </div>

    <!-- Slider -->
    <div class="overflow-hidden" id="podcast-slider-container">
      <div class="flex transition-transform duration-500 ease-out gap-6" id="podcast-track">
        
        <!-- Card 1 -->
        <div class="w-full md:w-1/2 lg:w-1/3 flex-shrink-0">
          <div class="group cursor-pointer">
            <div class="relative rounded-3xl overflow-hidden mb-6 aspect-video">
              <img src="/images/landing_page.png" alt="Podcast Thumbnail" class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105">
              <div class="absolute inset-0 bg-black/20 group-hover:bg-black/30 transition-colors"></div>
              <div class="absolute bottom-4 right-4 w-10 h-10 bg-teal-400 rounded-full flex items-center justify-center text-black">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" stroke="none">
                  <polygon points="5 3 19 12 5 21 5 3"></polygon>
                </svg>
              </div>
            </div>
            <div class="flex justify-between items-center text-xs font-bold text-gray-500 mb-3 uppercase tracking-wide">
              <span>33 Mins</span>
              <span class="bg-teal-50 text-teal-600 px-2 py-1 rounded">23 May</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
              Inside the Minds of CMOs: What's ACTUALLY Working on Social Media in 2025
            </h3>
            <p class="text-gray-600 text-sm line-clamp-2">
              CMOs are under pressure to deliver performance, fast. What questions are they asking most, and what strategies are actually working?
            </p>
          </div>
        </div>

        <!-- Card 2 -->
        <div class="w-full md:w-1/2 lg:w-1/3 flex-shrink-0">
          <div class="group cursor-pointer">
            <div class="relative rounded-3xl overflow-hidden mb-6 aspect-video">
              <img src="/images/landing_page.png" alt="Podcast Thumbnail" class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105">
              <div class="absolute inset-0 bg-black/20 group-hover:bg-black/30 transition-colors"></div>
              <div class="absolute bottom-4 right-4 w-10 h-10 bg-teal-400 rounded-full flex items-center justify-center text-black">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" stroke="none">
                  <polygon points="5 3 19 12 5 21 5 3"></polygon>
                </svg>
              </div>
            </div>
            <div class="flex justify-between items-center text-xs font-bold text-gray-500 mb-3 uppercase tracking-wide">
              <span>40 Mins</span>
              <span class="bg-teal-50 text-teal-600 px-2 py-1 rounded">23 May</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
              Do Influencers Make Good Entrepreneurs? How Creators Brands Are Changing the Game
            </h3>
            <p class="text-gray-600 text-sm line-clamp-2">
              The rise of influencer-led brands is reshaping how creators build long-term income streams.
            </p>
          </div>
        </div>

        <!-- Card 3 -->
        <div class="w-full md:w-1/2 lg:w-1/3 flex-shrink-0">
          <div class="group cursor-pointer">
            <div class="relative rounded-3xl overflow-hidden mb-6 aspect-video">
              <img src="/images/landing_page.png" alt="Podcast Thumbnail" class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105">
              <div class="absolute inset-0 bg-black/20 group-hover:bg-black/30 transition-colors"></div>
              <div class="absolute bottom-4 right-4 w-10 h-10 bg-teal-400 rounded-full flex items-center justify-center text-black">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" stroke="none">
                  <polygon points="5 3 19 12 5 21 5 3"></polygon>
                </svg>
              </div>
            </div>
            <div class="flex justify-between items-center text-xs font-bold text-gray-500 mb-3 uppercase tracking-wide">
              <span>34 Mins</span>
              <span class="bg-teal-50 text-teal-600 px-2 py-1 rounded">23 May</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
              ChatGPT Is About To Take Over Online Shopping
            </h3>
            <p class="text-gray-600 text-sm line-clamp-2">
              The rise of AI and ChatGPT is changing the way we approach Paid Media; but how can marketers stay ahead?
            </p>
          </div>
        </div>

        <!-- Card 4 -->
        <div class="w-full md:w-1/2 lg:w-1/3 flex-shrink-0">
          <div class="group cursor-pointer">
            <div class="relative rounded-3xl overflow-hidden mb-6 aspect-video">
              <img src="/images/landing_page.png" alt="Podcast Thumbnail" class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105">
              <div class="absolute inset-0 bg-black/20 group-hover:bg-black/30 transition-colors"></div>
              <div class="absolute bottom-4 right-4 w-10 h-10 bg-teal-400 rounded-full flex items-center justify-center text-black">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" stroke="none">
                  <polygon points="5 3 19 12 5 21 5 3"></polygon>
                </svg>
              </div>
            </div>
            <div class="flex justify-between items-center text-xs font-bold text-gray-500 mb-3 uppercase tracking-wide">
              <span>48 Mins</span>
              <span class="bg-teal-50 text-teal-600 px-2 py-1 rounded">23 May</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-2 group-hover:text-teal-600 transition-colors">
              How to Rank on Google SGE (Search Generative Experience)
            </h3>
            <p class="text-gray-600 text-sm line-clamp-2">
              SEO is constantly changing. With Google's new SGE, how can we ensure our content still ranks?
            </p>
          </div>
        </div>

      </div>
    </div>

    <!-- Dots -->
    <div class="flex justify-center gap-2 mt-12" id="podcast-dots">
       <button class="w-2 h-2 rounded-full bg-teal-400 transition-colors" data-index="0" aria-label="Slide 1"></button>
       <button class="w-2 h-2 rounded-full bg-gray-300 transition-colors" data-index="1" aria-label="Slide 2"></button>
       <button class="w-2 h-2 rounded-full bg-gray-300 transition-colors" data-index="2" aria-label="Slide 3"></button>
       <button class="w-2 h-2 rounded-full bg-gray-300 transition-colors" data-index="3" aria-label="Slide 4"></button>
    </div>
  </div>
</section>

<script>
  // Services Slider Logic
  function initServicesSlider() {
    const track = document.getElementById('services-track');
    const dots = document.querySelectorAll('#services-dots button');
    const nextBtn = document.getElementById('services-next-btn');
    
    if (!track || !dots.length || !nextBtn) return;

    let currentIndex = 0;
    const totalSlides = dots.length;

    function updateSlider() {
      const isDesktop = window.matchMedia('(min-width: 1024px)').matches;
      const isTablet = window.matchMedia('(min-width: 768px)').matches;
      
      let itemsVisible = 1;
      if (isDesktop) itemsVisible = 3;
      else if (isTablet) itemsVisible = 2;
      
      const percentage = (100 / itemsVisible) * currentIndex;
      track.style.transform = `translateX(-${percentage}%)`;
      
      dots.forEach((dot, idx) => {
        if (idx === currentIndex) {
          dot.classList.remove('bg-gray-300');
          dot.classList.add('bg-teal-400');
        } else {
          dot.classList.add('bg-gray-300');
          dot.classList.remove('bg-teal-400');
        }
      });
    }

    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        currentIndex = index;
        updateSlider();
      });
    });

    nextBtn.addEventListener('click', () => {
      currentIndex = (currentIndex + 1) % totalSlides;
      updateSlider();
    });
    
    // Add touch/swipe support for services slider
    let touchStartX = 0;
    let touchEndX = 0;
    
    track.addEventListener('touchstart', (e) => {
      touchStartX = e.changedTouches[0].screenX;
    });
    
    track.addEventListener('touchend', (e) => {
      touchEndX = e.changedTouches[0].screenX;
      handleSwipe();
    });
    
    function handleSwipe() {
      const swipeThreshold = 50;
      const diff = touchStartX - touchEndX;
      
      if (Math.abs(diff) > swipeThreshold) {
        if (diff > 0) {
          // Swipe left - next slide
          currentIndex = (currentIndex + 1) % totalSlides;
        } else {
          // Swipe right - previous slide
          currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
        }
        updateSlider();
      }
    }
    
    window.addEventListener('resize', updateSlider);
  }

  // Podcast Slider Logic
  function initPodcastSlider() {
    const track = document.getElementById('podcast-track');
    const dots = document.querySelectorAll('#podcast-dots button');
    
    if (!track || !dots.length) return;

    let currentIndex = 0;
    const totalSlides = dots.length;

    function updateSlider() {
      const isDesktop = window.matchMedia('(min-width: 1024px)').matches;
      const isTablet = window.matchMedia('(min-width: 768px)').matches;
      
      let itemsVisible = 1;
      if (isDesktop) itemsVisible = 3;
      else if (isTablet) itemsVisible = 2;
      
      const percentage = (100 / itemsVisible) * currentIndex;
      track.style.transform = `translateX(-${percentage}%)`;
      
      dots.forEach((dot, idx) => {
        if (idx === currentIndex) {
          dot.classList.remove('bg-gray-300');
          dot.classList.add('bg-teal-400');
        } else {
          dot.classList.add('bg-gray-300');
          dot.classList.remove('bg-teal-400');
        }
      });
    }

    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        currentIndex = index;
        updateSlider();
      });
    });
    
    // Add touch/swipe support for podcast slider
    let touchStartX = 0;
    let touchEndX = 0;
    
    track.addEventListener('touchstart', (e) => {
      touchStartX = e.changedTouches[0].screenX;
    });
    
    track.addEventListener('touchend', (e) => {
      touchEndX = e.changedTouches[0].screenX;
      handleSwipe();
    });
    
    function handleSwipe() {
      const swipeThreshold = 50;
      const diff = touchStartX - touchEndX;
      
      if (Math.abs(diff) > swipeThreshold) {
        if (diff > 0) {
          // Swipe left - next slide
          currentIndex = (currentIndex + 1) % totalSlides;
        } else {
          // Swipe right - previous slide
          currentIndex = (currentIndex - 1 + totalSlides) % totalSlides;
        }
        updateSlider();
      }
    }
    
    window.addEventListener('resize', updateSlider);
  }

  document.addEventListener('DOMContentLoaded', () => {
    initServicesSlider();
    initPodcastSlider();
  });
</script>
